numpy>=1.20
torch>=2.0
# Optional: required only when scripts/notebook are run with --data_source torchvision
# torchvision>=0.15
# Optional: only used by the notebook for plotting and the mini-matrix table
# matplotlib>=3.5
# pandas>=1.5
# Optional: required by `python -m pytest`
# pytest>=7.0
